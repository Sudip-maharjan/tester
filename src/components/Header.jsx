import React from "react";

const Header = () => {
  return (
    <header>
      <div className="logo">Travel Route Finder</div>
      <nav>{/* Add navigation items if needed */}</nav>
    </header>
  );
};

export default Header;
